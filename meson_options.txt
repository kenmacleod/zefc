option(
  'use_filc',
  type : 'boolean',
  value : true,
  description : 'Expect the C++ toolchain to be Fil-C++ (see filc_cpp). Warn at configure time if not.',
)

option(
  'filc_cpp',
  type : 'string',
  value : '/home/macken01/pizlo/filc-0.678-linux-x86_64/build/bin/fil++',
  description : 'Fil-C++ driver (fil++). Used by tools/setup-build.sh; pass CXX= this path when running meson setup.',
)

option(
  'method_dispatch',
  type : 'combo',
  choices : ['ic', 'vtable'],
  value : 'ic',
  description : 'ZEFC_SEND* object path: ic = per-site method IC (guard + cached callee); vtable = isa_->slots[sel] every send. Immediate Double/Int32 short-circuit in both. Field IC unchanged.',
)

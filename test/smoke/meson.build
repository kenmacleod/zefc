# Golden corpus (`zef/`, `expected/`) and stdlib bootstrap (`runtime_init.cpp`,
# `generated/{cstr,int,double,array,loadable_modules}.cpp`) used by the compiler
# suite. No hand-lowered case_*.cpp harness — see --suite compiler and --suite abi.

fn crap(lambda)
{
    class Foo {
        readable x
    
        fn (inX)
        {
            lambda(inX)
            x = inX
        }
    }
    
    class Bar : Foo {
        readable y
        
        fn (inX, inY)
        {
            super(inX)
            y = inY
        }
    }
    
    fn test Bar(1, 2)
    
    test
    test
}

crap(fn (x) println(x))
